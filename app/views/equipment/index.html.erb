<script type = "text/javascript">
    <!--$(IL.setup)-->
</script>
<h1>Equipment list </h1>
<table class = "table table-striped table-bordered table-condensed" id = "itemTable">
    <thead>
        <strong><tr>
            <th id="header_name">
                <a id =sortby_name href = <%= home_path %>> Name </a> 
            </th> 
             <th id=header_item_type>
                <a id =sortby_item_type href = <%= home_path %>> Type </a> 
            <!--    <select multiple id = "item_type_filter" style="width: 110px;"> 
					<option value="All"> All </option>
                    <% for type in @item_types%>
						<option value=<%=type%>><%=type%></option>
					<%end%>
                </select> 
                -->    
				
				<div class=type_list style="display: none;">
					<%= check_box_tag "All"%>
					<!--<% for type in @item_types%>
						<option value=<%=type%>><%=type%></option>
					<%end%> -->
				</div>
            </th>
        </tr></strong>
    </thead>
    <tbody>
        <% for equip in @equipment %>
            <tr id = <%= equip.name.gsub(/ /, "_")%>>
                <td id=name> <%=link_to show_equipment_path(equip.name) do equip.name end %></td>
                <td id=item_type> <%=equip.item_type%> </td>
            </tr>
        <% end %>
    </tbody>
</table>